---
import { Scale } from 'lucide-react';
import Button from './design-system/Button.astro';
import { firmInfo } from '../data/firm';
---

<section
  class="relative flex min-h-[70vh] flex-col overflow-hidden bg-gradient-to-br from-navy-900 to-navy-950 text-white"
>
  <div
    class="pointer-events-none absolute -top-24 -right-24 h-96 w-96 rounded-full bg-scrim-navy blur-3xl"
    aria-hidden="true"
  >
  </div>
  <div
    class="pointer-events-none absolute bottom-0 -left-24 h-80 w-80 rounded-full bg-scrim-navy blur-3xl opacity-70"
    aria-hidden="true"
  >
  </div>
  <div
    class="pointer-events-none absolute top-1/2 right-8 hidden -translate-y-1/2 opacity-10 xl:block"
    aria-hidden="true"
  >
    <Scale size={320} stroke-width={1} />
  </div>

  <div class="relative flex flex-1 flex-col gap-lg px-sm py-xl md:px-lg">
    <!-- Centered logo and firm name -->
    <div class="flex flex-col items-center gap-sm">
      <span
        class="font-heading text-3xl tracking-widest uppercase text-gold-500 md:text-5xl"
        style="animation: parallaxFadeDeep 1s cubic-bezier(0.34, 1.56, 0.64, 1) both 0.1s;"
      >
        {firmInfo.name}
      </span>
      <span
        class="font-body text-sm tracking-[0.25em] uppercase text-navy-200"
        style="animation: parallaxFadeMid 0.9s cubic-bezier(0.34, 1.56, 0.64, 1) both 0.2s;"
      >
        Law. Strategy. Advocacy.
      </span>
    </div>

    <!-- Wide paragraph, left-aligned -->
    <p
      class="w-full max-w-3xl font-body text-body leading-relaxed text-navy-100"
      style="animation: parallaxFadeShallow 0.8s cubic-bezier(0.34, 1.56, 0.64, 1) both 0.3s;"
    >
      From complex commercial disputes to deeply personal matters, MAKLAID pairs rigorous advocacy
      with clear, honest advice — so you always know where you stand and what comes next.
    </p>

    <!-- Buttons, left-aligned -->
    <div
      class="flex flex-col gap-sm sm:flex-row"
      style="animation: parallaxFadeFront 0.7s cubic-bezier(0.34, 1.56, 0.64, 1) both 0.4s;"
    >
      <Button href="/contact" variant="secondary" size="lg">Schedule Consultation</Button>
      <Button
        href="/services"
        size="lg"
        class="border border-line-white-strong bg-transparent text-white hover:bg-hover-white-strong"
      >
        Learn More
      </Button>
    </div>
  </div>
</section>
